using System;

namespace ArtFrameCore.Modules
{
    /// <summary>
    /// A visual UI panel/box that renders a solid background.
    /// </summary>
    public class Frame : Element
    {
        // Frames have the same baseline behaviors as Element, but specialize in rendering standard visual containers.
    }
}
